apiVersion: v1
kind: Service
metadata:
  name: {{ include "mvn-helm.fullname" . }}
  labels:
    {{- include "mvn-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mvn-helm.selectorLabels" . | nindent 4 }}
